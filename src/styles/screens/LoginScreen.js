import {StyleSheet} from 'react-native';
import {FLEX, COLOR} from './../styleHelpers';

const styles = StyleSheet.create({
  container: {
    flex: FLEX.flexOne,
    backgroundColor: COLOR.white,
  },
});

export default styles;
